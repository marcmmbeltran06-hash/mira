{
  "name": "@autowp/event-bus",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@autowp/shared": "workspace:*",
    "@autowp/core-domain": "workspace:*"
  }
}
